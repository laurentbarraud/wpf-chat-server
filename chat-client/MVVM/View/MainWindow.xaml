<Window x:Name="MainWindow1" x:Class="chat_client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:chat_client" xmlns:viewmodel="clr-namespace:chat_client.MVVM.ViewModel"
        mc:Ignorable="d"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="WPF Chat Server" Height="474" Width="816" WindowStartupLocation="CenterScreen" 
        Loaded="MainWindow_Loaded" ResizeMode="CanMinimize" Icon="/Resources/chat-client-icon.ico" 
        TabIndex="12" StateChanged="MainWindow1_StateChanged" Closing="MainWindow1_Closing" 
        PreviewKeyDown="MainWindow1_PreviewKeyDown">
    <Window.Resources>

        <!-- TaskbarIcon declaration -->
        <tb:TaskbarIcon x:Key="TrayIcon"
                    IconSource="/Resources/calc-icon.ico"
                    ToolTipText="Windows Calculator"
                    MenuActivation="RightClick"
                    TrayMouseDoubleClick="TrayIcon_TrayMouseDoubleClick" />

        <!-- Slide animation for emoji popup -->
        <Storyboard x:Key="SlideInStoryboard">
            <DoubleAnimation Storyboard.TargetName="popupEmojiPanel"
                     Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                     From="-200" To="0"
                     Duration="0:0:0.3"
                     AccelerationRatio="0.2"
                     DecelerationRatio="0.8" />
        </Storyboard>

        <!-- Slide-out animation for the emoji panel.
        Moves the panel horizontally off-screen to the left with smooth acceleration and deceleration. -->
        <Storyboard x:Key="SlideOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="popupEmojiPanel"
                     Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                     From="0" To="-480"
                     Duration="0:0:0.4"
                     AccelerationRatio="0.6"
                     DecelerationRatio="0.4" />
        </Storyboard>

        <!-- Pulse animation triggered when a public key is transmitted.
        Provides subtle visual feedback on encryption activation or refresh. -->
        <Storyboard x:Key="EncryptionPulseAnimation">
            <DoubleAnimation
            Storyboard.TargetName="imgEncryptionStatus"
            Storyboard.TargetProperty="RenderTransform.ScaleX"
            From="1.0" To="1.2" Duration="0:0:0.15" AutoReverse="True" />
            <DoubleAnimation
            Storyboard.TargetName="imgEncryptionStatus"
            Storyboard.TargetProperty="RenderTransform.ScaleY"
            From="1.0" To="1.2" Duration="0:0:0.15" AutoReverse="True" />
        </Storyboard>

        <!-- Slide-in and fade-out animation for encryption banner -->
        <Storyboard x:Key="ShowPopupBannerStoryboard">
            <DoubleAnimation Storyboard.TargetName="popupBanner"
                     Storyboard.TargetProperty="Opacity"
                     From="0" To="1"
                     Duration="0:0:0.3" />
            <DoubleAnimation Storyboard.TargetName="popupBanner"
                     Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                     From="-20" To="0"
                     Duration="0:0:0.3" />
        </Storyboard>

        <Storyboard x:Key="HidePopupBannerStoryboard">
            <DoubleAnimation Storyboard.TargetName="popupBanner"
                     Storyboard.TargetProperty="Opacity"
                     From="1" To="0"
                     Duration="0:0:0.3" BeginTime="0:0:2" />
            <DoubleAnimation Storyboard.TargetName="popupBanner"
                     Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                     From="0" To="-20"
                     Duration="0:0:0.3" BeginTime="0:0:2" />
        </Storyboard>

    </Window.Resources>
    <Window.DataContext>
        <viewmodel:MainViewModel/>
    </Window.DataContext>

    <Grid x:Name="grdMain" Background="{DynamicResource BackgroundBrush}">

        <!-- Always start with structural definitions -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Reusable popup banner for status messages -->
        <Border x:Name="popupBanner"
                Background="#FF2E8B57"
                Height="20"
                VerticalAlignment="Top"
                HorizontalAlignment="Stretch"
                Opacity="0"
                Visibility="Collapsed"
                RenderTransformOrigin="0.5,0.0"
                Panel.ZIndex="99">
            <Border.RenderTransform>
                <TranslateTransform Y="-20" />
            </Border.RenderTransform>

            <DockPanel LastChildFill="True" VerticalAlignment="Center" Margin="5,0">
                <Image x:Name="popupIcon"
                       Width="14" Height="14"
                       Margin="0,0,6,0"
                       VerticalAlignment="Center"
                       Visibility="Collapsed" />
                
                <TextBlock x:Name="popupText"
                           Foreground="White"
                           FontSize="12"
                           VerticalAlignment="Center" />
            </DockPanel>
        </Border>

        <DockPanel x:Name="dpnLeft">
            <Grid DockPanel.Dock="Top" Height="25">

                <!-- TextBox -->
                <TextBox x:Name="txtUsername"
                         Background="Transparent"
                         Foreground="{DynamicResource ForegroundBrush}" 
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         TextChanged="txtUsername_TextChanged"
                         FontSize="14"
                         MaxLines="1"
                         MaxLength="50"
                         TabIndex="0"
                         PreviewKeyDown="txtUsername_PreviewKeyDown"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Left"
                         Width="200"
                         Height="25"/>
                <Image x:Name="imgUsernameWatermark"
                       Stretch="Uniform"
                       IsHitTestVisible="False"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Width="{Binding ActualWidth, ElementName=txtUsername}"
                       Height="{Binding ActualHeight, ElementName=txtUsername}" ScrollViewer.VerticalScrollBarVisibility="Disabled" />

                <!-- Watermark -->
            </Grid>

            <Grid DockPanel.Dock="Top" Height="25">

                <!-- TextBox -->
                <TextBox x:Name="txtIPAddress"
                         Background="Transparent"
                         Foreground="{DynamicResource ForegroundBrush}" 
                         Text="{Binding IPAddressOfServer}"
                         TextChanged="txtIPAddress_TextChanged"
                         FontSize="14"
                         MaxLines="1"
                         MaxLength="15"
                         TabIndex="1"
                         PreviewKeyDown="txtIPAddress_PreviewKeyDown"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Left"
                         Width="200"
                         Height="25"/>
                <Image x:Name="imgIPAddressWatermark"
                       Stretch="Uniform"
                       IsHitTestVisible="False"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Width="{Binding ActualWidth, ElementName=txtIPAddress}"
                       Height="{Binding ActualHeight, ElementName=txtIPAddress}" ScrollViewer.VerticalScrollBarVisibility="Disabled" />
                <!-- Watermark -->
            </Grid>

            <Button x:Name="cmdConnectDisconnect" Height="25"
                    Background="{DynamicResource PrimaryBrush}"
                    Foreground="{DynamicResource ForegroundBrush}"
                    DockPanel.Dock="Top"
                    Click="cmdConnectDisconnect_Click" TabIndex="2" IsEnabled="False"/>
            <StackPanel DockPanel.Dock="Top"
                Orientation="Vertical"
                Margin="0,0,0,0" Height="356">

                <ListView x:Name="lstUsersConnected"
                    ItemsSource="{Binding Users}"
                    TabIndex="7"
                    Height="324"
                    Margin="0,0,0,0"
                    Background="{DynamicResource BackgroundBrush}"
                    Foreground="{DynamicResource ForegroundBrush}"
                    BorderBrush="{DynamicResource PrimaryBrush}"
                    BorderThickness="1"
                    SelectionMode="Single" SelectedIndex="3">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="4" />
                            <Setter Property="Margin" Value="1" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource BackgroundBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Username}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel x:Name="spnToolbar"
                            Orientation="Horizontal"
                            Height="32"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Margin="0,0,0,0">

                    <!-- Toggle button to switch theme -->
                    <ToggleButton x:Name="ThemeToggle"
                                  Width="60"
                                  Height="32"
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0"
                                  Checked="ThemeToggle_Checked"
                                  Unchecked="ThemeToggle_Unchecked" TabIndex="6">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Grid>
                                    <!-- Rounded background -->
                                    <Border x:Name="SwitchBackground"
                                        CornerRadius="16"
                                        Background="{DynamicResource BackgroundBrush}"
                                        BorderBrush="{DynamicResource PrimaryBrush}"
                                        BorderThickness="1" />

                                    <!-- Thumb with icon -->
                                    <Grid x:Name="ThumbContainer"
                                      Width="28" Height="28"
                                      VerticalAlignment="Center"
                                      Margin="2,2,30,2">
                                        <!-- Default: left -->
                                        <Ellipse Fill="{DynamicResource PrimaryBrush}" />

                                        <Image x:Name="SwitchIcon"
                                           Source="/Resources/sun.png"
                                           Width="18" Height="18"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                                    </Grid>
                                </Grid>

                                <ControlTemplate.Triggers>
                                    <!-- Checked: move thumb to right + change icon -->
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="SwitchIcon" Property="Source" Value="/Resources/moon.png" />
                                        <Setter TargetName="ThumbContainer" Property="Margin" Value="30,2,2,2" />
                                    </Trigger>

                                    <!-- Unchecked: move thumb to left + change icon -->
                                    <Trigger Property="IsChecked" Value="False">
                                        <Setter TargetName="SwitchIcon" Property="Source" Value="/Resources/sun.png" />
                                        <Setter TargetName="ThumbContainer" Property="Margin" Value="2,2,30,2" />
                                    </Trigger>

                                    <!-- Hover effect -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="SwitchBackground" Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                    </Trigger>

                                    <!-- Disabled state -->
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="SwitchBackground" Property="Opacity" Value="0.4" />
                                        <Setter TargetName="ThumbContainer" Property="Opacity" Value="0.4" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>

                    <!-- Settings Button -->
                    <Button x:Name="cmdSettings"
                              Width="32"
                              Height="32"
                              Background="Transparent"
                              BorderThickness="0"
                              VerticalAlignment="Center"
                              ToolTip="Settings" TabIndex="7" Click="cmdSettings_Click">
                        <Image x:Name="imgSettings" 
                               Source="/Resources/settings.png"
                               Stretch="Uniform"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center" />
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.8"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                </StackPanel>
            </StackPanel>
        </DockPanel>

        <StackPanel x:Name="spnCenter" Grid.Column="1" Visibility="Collapsed">

            <ListView x:Name="lstMessagesReceived"
                        Height="382"
                        ItemsSource="{Binding Messages}"
                        Background="{DynamicResource BackgroundBrush}"
                        Foreground="{DynamicResource ForegroundBrush}"
                        BorderThickness="0" TabIndex="6">

                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                        <Setter Property="Padding" Value="4" />
                        <Setter Property="Margin" Value="1" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource BackgroundBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   TextWrapping="Wrap"
                                   FontFamily="Segoe UI Emoji" />
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>

            <StackPanel x:Name="spnEmojiPanel" Orientation="Horizontal" Height="20">
                <Button x:Name="cmdEmojiPanel"
                        Height="{Binding ActualHeight, ElementName=spnEmojiPanel}"
                        Width="{Binding ActualHeight, ElementName=spnEmojiPanel}"
                        Background="Transparent"
                        BorderThickness="0"
                        VerticalAlignment="Center"
                        ToolTip="Emoji bar"
                        TabIndex="5"
                        Click="cmdEmojiPanel_Click"
                        Margin="4,0,4,0">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Image x:Name="imgEmojiPanel" 
                           Source="/Resources/right-arrow.png"
                           Stretch="Uniform"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Width="14" />
                </Button>

                <Image x:Name="imgEncryptionStatus"
                       Source="/Resources/encrypted-disabled.png"
                       Width="15" Height="15"
                       DockPanel.Dock="Left"
                       Margin="0,0,0,0"
                       VerticalAlignment="Center"
                       Visibility="Collapsed"
                       ToolTip="Encryption enabled">
                    <Image.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1" />
                    </Image.RenderTransform>
                </Image>

                <!-- This Popup is linked to the cmdEmojiPanel button and opens right with a slide animation. -->
                <!-- The emojis are bound to an EmojiList list defined in code-behind. -->
                <Popup x:Name="popupEmojiPanel"
                       Placement="Custom"
                       CustomPopupPlacementCallback="OnCustomPopupPlacement"
                       PlacementTarget="{Binding ElementName=txtMessageToSend}"
                       StaysOpen="False"
                       AllowsTransparency="True"
                       PopupAnimation="Slide"
                       Closed="PopupEmojiPanel_Closed">

                    <Popup.RenderTransform>
                        <TranslateTransform X="0" />
                    </Popup.RenderTransform>

                    <Border Background="{DynamicResource BackgroundBrush}"
                            BorderBrush="{DynamicResource PrimaryBrush}"
                            BorderThickness="1"
                            Padding="4"
                            CornerRadius="6"
                            Height="{Binding EmojiPanelHeight, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                            Width="460">

                        <!-- Adjusted layout for 13 emojis + arrows with vertical centering -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <!-- Left arrow -->
                                <ColumnDefinition Width="*" />
                                <!-- Emoji list -->
                                <ColumnDefinition Width="Auto" />
                                <!-- Right arrow -->
                            </Grid.ColumnDefinitions>

                            <!-- Left scroll button -->
                            <Button x:Name="btnScrollLeft"
                                    Grid.Column="0"
                                    Width="20"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    MouseEnter="btnScrollLeft_MouseEnter"
                                    MouseLeave="btnScrollLeft_MouseLeave"
                                    ToolTip="Scroll left">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Image Source="/Resources/left-arrow.png" Stretch="Uniform" />
                            </Button>
                            <!-- Scrollable emoji panel -->
                            <ScrollViewer x:Name="emojiScrollViewer"
                                          Grid.Column="1"
                                          VerticalAlignment="Center"
                                          MinHeight="48"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Disabled"
                                          CanContentScroll="True">
                                <ItemsControl x:Name="emojiItems"
                                                ItemsSource="{Binding EmojiList}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Width="20"
                                                    Height="{Binding ActualHeight, ElementName=spnEmojiPanel}"
                                                    Margin="2"
                                                    Padding="0"
                                                    Background="{DynamicResource PrimaryBrush}"
                                                    Foreground="{DynamicResource ForegroundBrush}"
                                                    BorderThickness="0"
                                                    VerticalAlignment="Center"
                                                    Click="EmojiButton_Click">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="4"
                                                                VerticalAlignment="Stretch"
                                                                HorizontalAlignment="Stretch">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center" />
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                                <TextBlock Text="{Binding}" FontSize="14"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center" />
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- Right scroll button -->
                            <Button x:Name="btnScrollRight"
                                    Grid.Column="2"
                                    Width="20"
                                    Height="{Binding ActualHeight, ElementName=spnEmojiPanel}"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    MouseEnter="btnScrollRight_MouseEnter"
                                    MouseLeave="btnScrollRight_MouseLeave"
                                    ToolTip="Scroll right">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Image Source="/Resources/right-arrow.png" Stretch="Uniform" />
                            </Button>
                        </Grid>
                    </Border>
                </Popup>

            </StackPanel>

            <StackPanel x:Name="spnDown" Orientation="Horizontal">
                <TextBox x:Name="txtMessageToSend" Height="32"
                         Width="545"
                         Background="{DynamicResource BackgroundBrush}"
                         Foreground="{DynamicResource ForegroundBrush}"
                         VerticalContentAlignment="Center"
                         Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}" TextChanged="txtMessageToSend_TextChanged" MaxLines="5" TextWrapping="Wrap" MaxLength="500" PreviewKeyDown="txtMessageToSend_PreviewKeyDown" TabIndex="3" FontFamily="Segoe UI Emoji"/>
                <Button x:Name="cmdSend"
                        Height="32"
                        Width="48"
                        Click="cmdSend_Click"
                        IsEnabled="False"
                        Background="{DynamicResource PrimaryBrush}"
                        Foreground="{DynamicResource ForegroundBrush}" TabIndex="4">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                                    <Setter Property="Opacity" Value="0.6" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Image Source="/Resources/send.png" Stretch="Uniform"/>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
