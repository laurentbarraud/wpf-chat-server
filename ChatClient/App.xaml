<Application x:Class="ChatClient.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:ChatClient.Converters"
             xmlns:tb="http://www.hardcodet.net/taskbar">

    <Application.Resources>

        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>

                <!-- Default theme loaded at startup (light mode) -->
                <ResourceDictionary Source="Themes/LightTheme.xaml" />

            </ResourceDictionary.MergedDictionaries>

            <!-- Built‑in WPF converter -->
            <BooleanToVisibilityConverter x:Key="BoolToVis" />

            <!-- Custom converters -->
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <conv:LeftOffsetToMarginConverter x:Key="LeftOffsetToMarginConverter" />
            <conv:SlightLightenConverter x:Key="SlightLightenConverter" BoostFactor="0.06"/>

            <!-- Interactive label style -->
            <Style x:Key="InteractiveTextBlockStyle" TargetType="Label">
                <Setter Property="Focusable" Value="True" />
                <Setter Property="IsTabStop" Value="True" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Cursor" Value="Arrow" />
                <Setter Property="Foreground" Value="WhiteSmoke" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Label">
                            <Border Background="{TemplateBinding Background}">
                                <TextBlock x:Name="PART_Text"
                                           Text="{TemplateBinding Content}"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontSize="{TemplateBinding FontSize}"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="PART_Text"
                                            Property="Foreground"
                                            Value="{DynamicResource AccentBrush}" />
                                    <Setter Property="Cursor" Value="Hand" />
                                </Trigger>

                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="PART_Text"
                                            Property="Foreground"
                                            Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Unique tray icon for the whole application -->
            <tb:TaskbarIcon x:Key="TrayIcon"
                            IconSource="pack://application:,,,/Resources/calc-icon.ico"
                            ToolTipText="Chat client"
                            Visibility="Collapsed" />

        </ResourceDictionary>

    </Application.Resources>
</Application>
